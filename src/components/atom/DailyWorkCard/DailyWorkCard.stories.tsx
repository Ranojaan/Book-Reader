import { DailyWorkCard } from './DailyWorkCard';

export default {
  component: DailyWorkCard,
};

export const Primary = {
  args: {
   
  },
};

export const DailyWorkCardSection = {
  args: {
    
  },
};
