import React from 'react';
import { TopBooks } from './PopularBooksContainer'; // Adjust the import path if necessary

export default {
  component: TopBooks,
  title: 'Molecules/PopularBooks', 
};

export const Default = () => <TopBooks />;
